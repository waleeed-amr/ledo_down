{
  "name": "ledo-downloader-mobile",
  "version": "1.0.0",
  "description": "Ledo Downloader - Mobile App for Android (Capacitor)",
  "author": "Waleed Amr",
  "license": "ISC",
  "scripts": {
    "sync": "node sync.js && npx cap sync android",
    "open": "npx cap open android",
    "build": "node sync.js && npx cap sync android && npx cap build android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.1",
    "@capacitor/browser": "^8.0.4",
    "@capacitor/cli": "^7.4.1",
    "@capacitor/core": "^7.4.1",
    "@capacitor/local-notifications": "^7.0.7"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
